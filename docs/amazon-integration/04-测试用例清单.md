# Amazon × 紫鸟 — 测试用例清单

> 环境：Java `:18080`、Vue `:5173`、`VITE_USE_TEMU_BACKEND=true`、紫鸟 WebDriver `:16851`、Agent 常驻  
> PoC 店铺：**YOTO美国账号** `browserId=16505337258263`  
> 关联回归：[05-回归基准.md](./05-回归基准.md)

## 环境矩阵

| 模式 | 配置 | 前置 |
|------|------|------|
| Backend+Agent | 后端开 + Agent 在线 + 紫鸟 WebDriver | 见 README PoC 命令 |
| Backend 无 Agent | 后端开、Agent 停 | 测错误提示与无 Demo 泄漏 |
| Demo | `VITE_USE_TEMU_BACKEND=false` | 行为与现网一致 |

### 账号

| 角色 | 账号 | 用途 |
|------|------|------|
| Boss | `admin@crosshub.cn` | 绑定、Agent、刷新 |
| 员工 Amazon | `liuyang@yituo-outdoor.com` | scope、越权 |
| 紫鸟自动化 | `HangZhouYiTuo` @ 泰州亿拓户外有限公司 | 仅 Agent/WebDriver |

---

## 一、紫鸟 WebDriver（PY）

| ID | 场景 | 步骤 | 预期 | 结果 |
|----|------|------|------|------|
| ZN-01 | WebDriver 端口 | 开发者模式启动后 `netstat :16851` | LISTENING | PASS |
| ZN-02 | getBrowserList | `py scripts/probe_ziniao.py` | statusCode=0，≥1 店铺 | PASS |
| ZN-03 | 普通模式冲突 | 普通紫鸟运行时调 probe | 明确报错提示退出普通模式 | 待测 |
| ZN-04 | 错误凭据 | `.env` 错误密码 | statusCode=-10003 | 待测 |
| ZN-05 | startBrowser | 对 `16505337258263` start | debuggingPort>0 | 待测 |
| ZN-06 | stopBrowser | stop 后端口释放 | 可再次 start | 待测 |
| ZN-07 | IP 检测失败 | 过期设备店铺 | statusCode=2，err 含 IP | 待测 |

---

## 二、Agent API

| ID | 场景 | 步骤 | 预期 | 结果 |
|----|------|------|------|------|
| AG-01 | 注册节点 | Boss `POST /api/agent/register` | 返回 node_id + token | 待测 |
| AG-02 | 心跳 | Agent `POST /api/agent/heartbeat` | 200，`ziniao_online` 正确 | 待测 |
| AG-03 | 无 Token | heartbeat 无 `X-Agent-Token` | 401 | 待测 |
| AG-04 | 拉空任务 | `GET /api/agent/tasks` 无 pending | `[]` | 待测 |
| AG-05 | discover 任务 | Java 创建 discover → Agent 完成 | candidates 含 browserId | 待测 |
| AG-06 | 跨租户任务 | Agent A token 完成租户 B job | 403 | 待测 |

---

## 三、Amazon 绑定 API

| ID | 场景 | 步骤 | 预期 | 结果 |
|----|------|------|------|------|
| API-01 | discover | Boss `POST /api/amazon/ziniao/discover` | 202 或同步返回任务 id | 待测 |
| API-02 | candidates | `GET /api/amazon/ziniao/candidates` | 含 YOTO美国账号 | 待测 |
| API-03 | bind | `POST /api/amazon/ziniao/bind` browserId | platform_account 含 external_shop_id | 待测 |
| API-04 | 重复 bind | 同一 browserId 绑两次 | 409 或幂等更新 | 待测 |
| API-05 | 员工 bind | 员工 POST bind | 403 | 待测 |
| API-06 | 无 Token | 无 JWT | 401 | 待测 |

---

## 四、Amazon 同步 API

| ID | 场景 | 步骤 | 预期 | 结果 |
|----|------|------|------|------|
| API-10 | 触发 sync | `POST /api/amazon/sync` scope=account_health | 202 pending | 待测 |
| API-11 | 轮询成功 | GET job 直至 success | metrics_count≥1 | 待测 |
| API-12 | 并发冲突 | 同店同 scope 连续 POST | 第二次 409 | 待测 |
| API-13 | Agent 离线 | 停 Agent 后 POST sync | 503 AMAZON_AGENT_OFFLINE | 待测 |
| API-14 | 越权店铺 | 员工 POST 非 scope 店 | 403 | 待测 |
| API-15 | 跨租户 job | A 租户读 B 租户 jobId | 404 | 待测 |
| API-16 | 紫鸟未登录 | Seller Central 需 MFA | failed + NEEDS_MANUAL_LOGIN | 待测 |
| API-17 | GET daily | 成功后 `GET /api/amazon/daily` | account_metrics 非空 | 待测 |
| API-18 | GET insights | P2 `GET /api/amazon/insights` | products 结构正确 | 待测 |

---

## 五、前端 UI

| ID | 场景 | 步骤 | 预期 | 结果 |
|----|------|------|------|------|
| UI-01 | Demo 模式 | `VITE_USE_TEMU_BACKEND=false` | 仍展示 Demo 种子 | 待测 |
| UI-02 | 后端无绑定 | 后端模式未绑 Amazon | 提示绑定，无 Demo 消息 | 待测 |
| UI-03 | 紫鸟导入 | 账户绑定页导入 | 列表勾选 → 绑定成功 | 待测 |
| UI-04 | 刷新按钮 | Amazon 页点刷新 | loading → success Toast | 待测 |
| UI-05 | 账户状况 Tab | 同步后打开 | 指标非 demo_amazon_1 | 待测 |
| UI-06 | Agent 离线 | 停 Agent 刷新 | 错误 Toast，表格不恢复 Demo | 待测 |
| UI-07 | Boss 产品 Tab | P2 同步后 | TOP 列表或空态 | 待测 |
| UI-08 | 运营总览 | 首页 Amazon 卡片 | 待办数来自 API | 待测 |
| UI-09 | 员工门户 | liuyang 登录 | 仅 Amazon 菜单与 scope 店 | 待测 |

---

## 六、数据契约

| ID | 场景 | 预期 | 结果 |
|----|------|------|------|
| DC-01 | daily 字段 | `store_id` = platform_accounts.id | 待测 |
| DC-02 | account_metrics.status | ∈ normal/warning/critical | 待测 |
| DC-03 | snake_case 响应 | 前端 map 能读 `store_id` | 待测 |
| DC-04 | bind 请求 | `external_shop_id` snake_case 写入 | 待测 |
| DC-05 | 空 scope 结果 | 成功 job 但 0 行 | UI 空态，非 Demo | 待测 |

---

## 七、安全

| ID | 场景 | 预期 | 结果 |
|----|------|------|------|
| SEC-01 | `.env` 紫鸟密码 | 不在 git 仓库 | 待测 |
| SEC-02 | Agent token | 泄露后 rotate 旧 token 失效 | 待测 |
| SEC-03 | 日志脱敏 | Java/Python 日志无 password | 待测 |

---

## 八、与权限交叉

| ID | 场景 | 预期 | 参考 |
|----|------|------|------|
| X-01 | liuyang 菜单 | 含 amazon，不含 temu | permissions API-MENU-03 |
| X-02 | Amazon 越权读 | 非 scope 店 daily 空/403 | permissions API-DATA-01 |
| X-03 | crawl 类比 | 无 Amazon scope 不可 sync | 新建 API-14 |

---

## 执行记录模板

```
日期：
执行人：
环境：Java __ / Vue __ / 紫鸟 WebDriver __ / Agent __
通过：__ / 失败：__ / 跳过：__
备注：
```
